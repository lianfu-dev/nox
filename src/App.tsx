import React from 'react';

import AppRoot from '@commonComp/app-root';

interface IProps {}
interface IState {}

const App = (props: IProps, state: IState) => (
  <AppRoot />
);

export default App;
